# 项目指标

> W26 六个月检查点：2027-01-27。届时按 `docs/PLAN.md` §13.2 逐条评估继续维护、
> 缩减范围或成功退出。此文件每月更新一次。

## 2026-07-30 基线

| 指标 | 目标 | 当前值 | 状态 |
|---|---:|---:|---|
| SAFE 类语料有译文比例 | ≥ 90% | 340 / 740（45.9%） | 未达成 |
| 新版本自动 PR 响应 | < 6h | 尚无定时生产样本 | 待建立基线 |
| 外部贡献者 | ≥ 3 人 | 0 | 未达成 |
| 双端稳定 release | Desktop + CLI | Desktop `v0.1.0`；CLI 尚无 release | 未达成 |
| 严重事故 | 0 | 0 | 达成 |
| 术语词表 | ≥ 300 | 353 | 达成 |

SAFE 有译文比例是仓库原始统计，包含 400 条尚未被 A 层消费的 CLI `2.1.112`
SAFE 语料。它不等同于当前界面可见覆盖率。

## 当前质量状态

- Desktop：320 条 SAFE 已翻译并完成 `1.18286.0` 冒烟验证。
- CLI A 层：19 条 SAFE 已翻译，S1–S3 已通过。
- CLI 原生层：`2.1.220` 已建立 86 条帮助 surface 映射；C 层因单字节编码不能承载
  中文而停止，B 层 1 条等宽 spinner 译文已通过 S1–S3。
- UNKNOWN：84 条。
- 开放严重事故：0。

## 更新规则

每月记录：

1. 各产品 SAFE、已翻译、UNKNOWN、DANGER、FRAGILE 数量；
2. 新版本发现到自动 PR 创建的耗时；
3. 外部贡献者和首次贡献者成功率；
4. 双端发布状态；
5. 补丁导致 Claude 不可用的严重事故；
6. 术语数量和 CI 一致性结果。
