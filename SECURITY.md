# Security Policy

## 安全承诺

`claude-zh-bilingual` 只处理用户界面展示文案，不修改以下代码或配置：

- 鉴权、登录与凭证存储
- 计费、用量统计与限额
- 网络请求目标与代理配置
- 遥测与数据上报

补丁工具除下载官方原始包外不发起网络请求，不收集任何用户数据，不包含分析 SDK，也不会自动请求管理员或 sudo 权限。

## 发布要求

- 发布产物必须由 GitHub Actions 可复现构建。
- 每个 release asset 必须附带 SHA-256。
- Windows 单文件启动器只嵌入官方 Node 运行时、锁定依赖和本项目文件，不得嵌入
  Claude 二进制、ASAR、用户配置或凭证。
- 启动器释放的每个运行文件必须在执行前通过内置清单的长度与 SHA-256 校验。
- 修改前必须创建备份并打印备份路径。
- 安装、重打包或冒烟测试任一环节失败时必须自动还原。
- CLI 的三条冒烟测试或 Desktop 核心断言任一失败时禁止发布。

## 报告漏洞

请使用 GitHub 的私密漏洞报告功能提交安全问题。不要在公开 issue 中披露可利用细节、恶意语料或被篡改的发布产物。

报告中请包含：

- 受影响版本和平台
- 最小复现步骤
- 实际结果与预期结果
- 是否可能导致 Claude 无法启动、agent 行为退化或用户文件受损

收到有效报告后，维护者将优先复现和隔离问题。若确认语料投毒或 patcher 被篡改，将撤回受影响 release，并在修复和复盘完成前暂停发布。
